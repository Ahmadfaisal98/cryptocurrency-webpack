import React from 'react';
import MenuButton from './MenuButton';

export default {
  title: 'Design/Molecules/MenuButton',
  component: MenuButton,
};

const Template = (args) => <MenuButton {...args} />;

export const Default = Template.bind({});
